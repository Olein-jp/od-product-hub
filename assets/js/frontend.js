document.addEventListener('click',(event)=>{const button=event.target.closest('.odph-copy');if(!button)return;navigator.clipboard.writeText(button.dataset.license).then(()=>{const status=button.parentElement.querySelector('.odph-copy-status');status.textContent='ライセンスキーをコピーしました。';button.textContent='コピー済み';setTimeout(()=>{button.textContent='コピー';status.textContent='';},2000);});});
